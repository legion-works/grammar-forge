// Options/settings UI (React). Bridge URL + health check, remote-bridge opt-in,
// blocked sites, toggles, personal dictionary, privacy banner. Fleshed out in
// WS-B Task 9.
export function App() {
    return <main>GrammarForge Settings</main>
}
